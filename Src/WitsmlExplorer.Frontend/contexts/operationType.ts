enum OperationType {
  DisplayContextMenu,
  HideContextMenu,
  DisplayModal,
  HideModal,
  SetCheckedContentItems,
  AddToClipboard,
  ClearClipboard,
  SetTheme,
  SetTimeZone
}

export default OperationType;
